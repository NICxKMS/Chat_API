{
  "name": "chat-api-test-site",
  "version": "1.0.0",
  "description": "Test site for Chat API with Protocol Buffer support",
  "main": "server.js",
  "scripts": {
    "start": "nodemon start.js",
    "server": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 